# 里程碑 E 任务清单

> 状态：未完成阶段计划。
> 本文档描述的是 mapping 兼容层进一步退场、彻底切到 workflow published 真源后的清理工作。当前仓库尚未完成这一阶段。

> 注：本文档描述的是“映射进一步迁移到 SQLite published 源”的阶段目标。当前主分支运行结构尚未完成该阶段，现行配置源仍以 `data/config/` 为准。

## 目标范围

- 下线运行时代码对 `public/data/*mapping*.json`（旧路径）的依赖
- JSON 文件保留为导入导出用途，不再作为业务运行数据源
- 补齐上线前的兜底、监控、回滚能力

## 阶段目标

1. 运行链路只认 SQLite `published`。
2. 静态 JSON 不再参与前端运行时 fetch，也不再参与服务端运行时读取。
3. 保留迁移 / 导出 / 回滚工具链。

## 一、清理运行时 JSON 依赖

### 1. 修改 `/Users/aries/Dve/workspace/src/services/configLoader.ts`
1. 删除映射类静态 JSON fallback：
   - `/data/cylinder-mapping.json`
   - `/data/lock-fork-mapping.json`
   - `/data/packaging-mapping.json`
2. 映射 API 不可用时显式报错，不再静默回退。

### 2. 修改 `/Users/aries/Dve/workspace/server/routes/configData.js`
1. 删除或废弃 `GET /api/config/packaging-mapping`。
2. 如果需要兼容期，可先内部转发到 `/api/config/mappings/packaging/published`。

### 3. 全仓库搜索并清理旧读取点
1. 重点清理关键词：
   - `cylinder-mapping.json`
   - `lock-fork-mapping.json`
   - `packaging-mapping.json`
   - `/api/config/packaging-mapping`

## 二、下线旧兼容 API

### 1. 删除旧包装映射接口
1. 文件：`/Users/aries/Dve/workspace/server/routes/configData.js`
2. 若该文件仍承载 materials API，仅移除 packaging mapping 相关路由。

### 2. 更新服务端路由文档
1. 更新 `/Users/aries/Dve/workspace/docs/api.md`
2. 标注废弃接口：`/api/config/packaging-mapping`
3. 明确新标准接口：`/api/config/mappings/:type/published`

### 3. 删除前端旧兼容分支
1. 确保没有双读逻辑。

## 三、保留 JSON 作为运维工具

### 1. 保留三份原始 JSON 文件
1. `/Users/aries/Dve/workspace/public/data/packaging-mapping.json`（历史路径，当前为 `/Users/aries/Dve/workspace/data/config/packaging-mapping.json`）
2. `/Users/aries/Dve/workspace/public/data/cylinder-mapping.json`（历史路径，当前为 `/Users/aries/Dve/workspace/data/config/cylinder-mapping.json`）
3. `/Users/aries/Dve/workspace/public/data/lock-fork-mapping.json`（历史路径，当前为 `/Users/aries/Dve/workspace/data/config/lock-fork-mapping.json`）
4. 用途改为：首次导入参考、运维回滚参考、审计对比。

### 2. 如有必要，后续迁移到非运行目录
1. 可考虑：
   - `data/mapping-seeds/`
   - `server/scripts/seeds/`

### 3. 固化导入导出脚本
1. `db:migrate:mappings`
2. `db:export:mappings`

## 四、增加运行时健康检查

### 1. 新增 health API
1. 建议：`GET /api/config/mappings/health`
2. 返回：
   - 三类 mapping 是否都有 published
   - 当前 revision
   - updated_at
   - schema_version

### 2. 前端可显示当前 mapping revision
1. 建议在管理页或分析页显示当前 revision 概览。

## 五、补充监控与审计

### 1. 完善 unmatched 事件采集
1. 当前包装匹配仅有前端内存 unmatched 统计基础。
2. E 阶段建议落服务端存储或增加上传接口。

### 2. 发布 / 回滚审计可查询
1. `operator`
2. `change note`
3. `from_revision`
4. `to_revision`
5. `created_at`

### 3. 错误日志分类清晰
1. 配置加载失败
2. published 缺失
3. validator 失败
4. runtime adapter 失败

## 六、补齐回滚方案

### 1. 确认 `db:export:mappings` 可导出当前 published
### 2. 决定是否保留短期旧逻辑开关
### 3. 编写 `docs/mapping-rollback-runbook.md`
1. 何时触发回滚
2. 如何导出当前 published
3. 如何恢复旧 JSON
4. 如何冻结发布操作
5. 如何验证恢复成功

## 七、测试清理与增强

### 1. 更新 source guard
1. 原有 `/Users/aries/Dve/workspace/tests/packaging-mapping-source-guard.test.js` 需要改造。
2. 新断言：
   - 不允许运行时代码 fetch 静态 mapping JSON
   - 不允许使用旧包装接口

### 2. 新增“无 fallback”测试
1. 当 `/api/config/mappings/:type/published` 返回失败时：
   - 系统显式报错
   - 不再偷偷回落到 JSON

### 3. 新增 health check 测试
1. 三类 published 存在时返回成功。
2. 缺失任一类时返回异常状态或错误详情。

### 4. 回归全链路测试
1. `npm run type-check`
2. `npm test`
3. `npm run build`

## 八、文档更新

### 1. 更新 `/Users/aries/Dve/workspace/docs/api.md`
1. 删除旧运行接口说明。
2. 强调新标准 API。

### 2. 新增 `docs/mapping-runtime-cutover.md`
1. 记录切流步骤、验证项、回滚项。

### 3. 更新开发说明
1. 任何新增运行时需求不得再从 `public/data/*mapping*.json`（旧路径）读取。

## 九、里程碑 E 完成标准

1. 运行时代码不再读取 mapping JSON。
2. 旧包装映射 API 已删除或完全废弃。
3. published 成为唯一运行时来源。
4. 有健康检查、导出脚本、回滚文档。
5. source guard 能防止后续回退到旧模式。
6. 全链路测试通过。

## 整体执行顺序建议

1. 先完成批次 1：迁移前优化。
2. 再完成 A、B、C、D。
3. 最后做 E 的切流与清理。
