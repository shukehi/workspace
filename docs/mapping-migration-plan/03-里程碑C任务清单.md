# 里程碑 C 任务清单

> 状态：历史阶段任务记录。
> 文中关于“统一 published API 接入”“configLoader 收口”等目标已大部分落地，本文档主要保留阶段拆分和验收思路，不作为当前待办清单。

## 目标范围

- 前端与运行链路收口到统一 `published` API
- 不做管理台编辑能力
- 重点是“所有业务消费口径一致”

## 阶段目标

1. `configLoader` 不再直接依赖运行时 JSON 文件。
2. 采购生成、打印预览、物料分析三处都消费统一的 `published` 数据。
3. 保留 adapter 兼容层，避免一次性重写业务逻辑。

## 一、前端类型与 API 封装

### 1. 新增 `/Users/aries/Dve/workspace/src/types/mapping.ts`
1. `MappingType`
2. `MappingRevisionMeta`
3. `PublishedMappingEnvelope<T>`
4. `PackagingMappingPayload`
5. `CylinderMappingPayload`
6. `LockForkMappingPayload`

### 2. 新增 `src/services/mappingApi.ts`
1. `getPublishedMapping(type)`
2. `getPublishedPackagingMapping()`
3. `getPublishedCylinderMapping()`
4. `getPublishedLockForkMapping()`
5. 全部走 `/api/config/mappings/:type/published`
6. 统一错误处理

## 二、配置加载层收口

### 1. 修改 `/Users/aries/Dve/workspace/src/services/configLoader.ts`
1. 移除运行时对以下文件的直接读取：
   - `/data/cylinder-mapping.json`
   - `/data/lock-fork-mapping.json`
   - `/data/packaging-mapping.json`
2. 改为：
   - `loadCylinderMappingFromApi()`
   - `loadLockForkMappingFromApi()`
   - `loadPackagingMappingFromApi()`
3. 保留内存缓存，但新增：
   - `refreshMappings()`
   - `refreshMapping(type)`
4. loader 内只存 `payload`

### 2. 统一 published 响应解包
1. 例如：`this.packagingMapping = res.payload`
2. 下游模块不感知 `revision/schemaVersion`

### 3. 增加版本信息访问器
1. `getPackagingMappingMeta()`
2. `getCylinderMappingMeta()`
3. `getLockForkMappingMeta()`

## 三、前端 adapter 层

### 1. 新增 `src/services/mappings/mappingAdapter.ts`
1. `normalizePackagingMapping`
2. `normalizeCylinderMapping`
3. `normalizeLockForkMapping`

### 2. 统一包装映射结构
1. 不再兼容直接字典对象。
2. 只接受规范化后的包装映射 DTO。

## 四、包装链路收口

### 1. 修改 `/Users/aries/Dve/workspace/src/lib/packagingMatcher.ts`
1. 只接受规范化包装映射结构。
2. 删除直接字典对象兜底假设。
3. 保留 unmatched 计数逻辑。

### 2. 修改 `/Users/aries/Dve/workspace/src/lib/erp-engine/packagingTable.ts`
1. 只消费统一包装映射 DTO。
2. 删除 `PACKAGING_MAPPING.mappings || PACKAGING_MAPPING` 兼容写法。

### 3. 修改 `/Users/aries/Dve/workspace/src/features/procurement/packagingNameResolver.ts`
1. 只依赖统一映射结构。

### 4. 修改 `/Users/aries/Dve/workspace/src/features/procurement/prepareOrderDraft.ts`
1. 使用 loader / adapter 返回的包装映射。
2. 校验生成草稿时 supplier / external name 来源一致。

### 5. 修改 `/Users/aries/Dve/workspace/src/components/procurement/EditOrderDialog.vue`
1. 包装默认 supplier 与名称解析全部走统一映射。

## 五、锁芯链路收口

### 1. 定位锁芯提取与计算入口
1. 当前入口在 `/Users/aries/Dve/workspace/src/stores/useSourceStore.ts`
2. 继续追到 `extractCylinderData` 实现文件并改造。

### 2. 修改锁芯 extractor
1. 只消费 adapter 标准化后的锁芯映射结构。
2. 删除对静态 JSON 特有字段形状的隐式依赖。

### 3. 增加锁芯 smoke test
1. 验证迁移前后提取结果一致。

## 六、锁叉链路收口

### 1. 定位锁叉提取与计算入口
1. 当前入口在 `/Users/aries/Dve/workspace/src/stores/useSourceStore.ts`
2. 继续追到 `extractLockForkData` 实现文件。

### 2. 修改锁叉 extractor
1. 只消费 adapter 标准结构。
2. 不再直接依赖 JSON 原始布局。

### 3. 增加锁叉复杂规则回归样本
1. 锁叉是最容易口径漂移的部分，测试优先补。

## 七、采购生成链路收口

### 1. 修改 `/Users/aries/Dve/workspace/src/services/poGenerator.ts`
1. 确保运行前只从 `configLoader` 取映射。
2. packaging matcher 同步来源唯一。
3. cylinder / lock_fork group 构建来源唯一。

### 2. 检查 `src/services/po-rules/*`
1. 尤其 `/Users/aries/Dve/workspace/src/services/po-rules/packagingRule.ts`
2. 规则层只拿统一 DTO。

### 3. 增加 PO 结果一致性测试
1. supplier
2. external name
3. spec
4. grouping key
5. qty

## 八、物料分析链路收口

### 1. 修改 `/Users/aries/Dve/workspace/src/stores/useSourceStore.ts`
1. `calculateMaterials()` 前已加载 published mappings。
2. 建议：
   - `await configLoader.loadAll()`
   - `await configLoader.refreshMappings()`

### 2. 检查 `dataExtractors`
1. 目标文件在 `src/lib/erp-engine/dataExtractors*`
2. 三类映射统一入参契约。
3. extractor 内部不再自行 fetch 数据。

### 3. 增加分析页回归测试
1. `cylinders`
2. `lockForks`
3. `packaging`
4. 汇总结果一致

## 九、打印预览链路收口

### 1. 盘点打印预览页面的数据来源
1. 重点文件：
   - `/Users/aries/Dve/workspace/server/routes/print.js`
   - 打印页面对应前端组件

### 2. 推荐方案
1. 打印快照中固化已解析结果。
2. 不在 PDF 渲染时重新查最新 mapping。

### 3. 如果当前打印页仍会动态计算
1. 改为统一通过 loader 获取 published mapping。
2. 在创建 snapshot 时记录 revision meta。

### 4. 扩展打印测试
1. 参考：
   - `/Users/aries/Dve/workspace/tests/print-snapshot-route.test.js`
   - `/Users/aries/Dve/workspace/tests/pdf-route.test.js`
2. 增加 revision 固化或结果一致性断言。

## 十、前端测试

### 1. 修改 / 新增 `configLoader` 测试
1. 校验三类映射都从 `/api/config/mappings/:type/published` 读取。
2. 不再允许 fallback 到静态 JSON。

### 2. 扩展 source guard
1. 禁止以下运行时 fetch：
   - `/data/cylinder-mapping.json`
   - `/data/lock-fork-mapping.json`
   - `/data/packaging-mapping.json`

### 3. 新增运行时回归测试
1. `tests/mapping-runtime-source-guard.test.js`
2. `tests/po-generator-mapping-published.test.ts`
3. `tests/data-extractors-mapping-published.test.ts`

### 4. 维持现有 PO / 包装测试通过
1. `/Users/aries/Dve/workspace/tests/po-rule-packaging.test.ts`
2. `/Users/aries/Dve/workspace/tests/po-generator-integration.test.ts`

## 十一、文档与说明

### 1. 更新 `/Users/aries/Dve/workspace/docs/api.md`
1. 补 `published` 映射 API 用法。

### 2. 新增 `docs/mapping-runtime-contract.md`
1. 前端只消费 published。
2. DTO 结构。
3. loader 缓存与刷新规则。
4. 打印快照是否固化 revision。

## 十二、里程碑 C 完成标准

1. `configLoader` 三类映射全部改走 `/api/config/mappings/:type/published`。
2. 运行时代码不再直接 fetch 映射 JSON。
3. 采购生成、物料分析、打印预览三处映射来源一致。
4. 关键回归测试通过。
5. `npm run type-check`、`npm test`、`npm run build` 通过。

## 推荐开发顺序

1. `src/types/mapping.ts`
2. `src/services/mappingApi.ts`
3. `src/services/mappings/mappingAdapter.ts`
4. `/Users/aries/Dve/workspace/src/services/configLoader.ts`
5. packaging 链路
6. cylinder 链路
7. lock_fork 链路
8. `/Users/aries/Dve/workspace/src/services/poGenerator.ts`
9. print / snapshot 链路
10. 测试与 source guard
