# 库存管理优化方案

> 状态：**历史计划**。
> 请优先参考 [INVENTORY_OPTIMIZATION_PROGRESS.md](/Users/aries/Dve/workspace/INVENTORY_OPTIMIZATION_PROGRESS.md) 了解当前真实完成状态。
> 本文档保留为问题定义与原始方案参考，不再作为唯一执行依据。

## 目标

将库存模块从“主流程可用”提升到“账本一致、并发安全、可审计、可扩展”。

当前库存逻辑的主要问题不在页面交互，而在底层账本模型还没有完全收口：

- 总库存和库位库存存在两套账风险
- 入库、出库、冲销各自维护库存，规则分散
- 并发下库存扣减存在覆盖写风险
- 列表查询存在全量拉取后内存过滤的问题

## 当前主要风险

### 1. 总库存与库位库存可能不一致

当前 `PUT /inventory/:id` 会直接修改 `materials.stock_quantity`，但不会同步 `inventory_location_balances`，也不会生成调账流水。

这会导致：

- 总库存数字被改了，但库位分布没变
- 出库按库位余额校验时和库存页展示产生冲突
- 后续难以追溯是谁、为什么改了库存

### 2. 库存变更逻辑分散

当前入库、出库、冲销都分别维护：

- `materials.stock_quantity`
- `inventory_location_balances.quantity`

问题在于：

- 规则重复
- 后续修改容易漏改某一条链路
- 不同入口的边界条件可能逐渐漂移

### 3. 并发安全不足

当前库存更新主要是“读取旧值 -> 计算新值 -> update”。

在并发请求下可能出现：

- 多次出库同时读取同一旧值
- 校验基于旧库存通过
- 最终出现覆盖写、超扣或校验失真

### 4. 查询性能会随数据量恶化

库存总览和出库列表存在“先全量查出，再在内存中过滤/分页”的模式。

数据量增长后会出现：

- 列表接口变慢
- 分页不准确
- 导出与列表共用逻辑导致额外压力

## 优化原则

1. 库存只保留一套真实账本语义
2. 所有库存变更必须留下业务流水
3. 所有库存增减都走统一原子入口
4. 列表查询、过滤、分页尽量下沉到数据库

## 分阶段方案

## P0：先修账本一致性

### 目标

先把“总库存可被单独改写”的漏洞关掉，保证系统不会继续制造脏账。

### 方案

1. 废弃直接改总库存的模式

- 保留库存编辑接口时，只允许修改 `min_stock`
- 禁止直接修改 `stock_quantity`

2. 新增库存调账能力

新增接口建议：

- `POST /inventory-adjustments`

请求字段建议强制包含：

- `material_id`
- `warehouse_id`
- `location_id`
- `delta_quantity`
- `reason`
- `operator`
- `remark`

3. 调账时必须同步更新三类数据

- `materials.stock_quantity`
- `inventory_location_balances.quantity`
- 一条调账流水记录

4. 前端库存页调整

- 去掉“直接编辑库存数”
- 保留“调整安全库存”
- 新增“发起调账”入口

5. 增加历史库存基线修复

- 先生成库存差异报告
- 对历史脏数据执行一次性基线修复
- 修复动作也写入统一库存流水
- 无法确认真实库位归属的差异，统一落到 `DEFAULT / UNASSIGNED`

### 结果

完成后，系统不再允许产生“总库存变了、库位余额没变”的新数据，同时可以收口现有历史差异。

## P1：统一库存变更模型

### 目标

把入库、出库、冲销、调账统一成一套库存变更机制。

### 方案

1. 抽出统一库存变更服务

建议新增：

- `server/services/inventory/inventory-movement.service.ts`

统一入口可以类似：

- `applyInventoryDelta(...)`

2. 统一入口负责的职责

- 校验仓库和库位有效性
- 查找或创建库位余额
- 校验余额不能为负
- 原子更新总库存和库位库存
- 写入库存流水

3. 所有库存变更入口统一接入

以下流程都不再直接手写库存加减：

- 采购入库
- 入库冲销
- 手工出库
- 出库冲销
- 手工调账

4. 定义统一来源类型

建议至少包含：

- `receipt_in`
- `receipt_reversal`
- `outbound`
- `outbound_reversal`
- `manual_adjustment`
- `baseline_repair`

### 结果

完成后，库存变更规则只有一份，实现成本和维护成本都会显著下降。

## P1：补并发安全

### 目标

保证同一物料、同一库位在并发操作下不会被超扣或写坏。

### 方案

1. 不再使用简单的“读旧值再写新值”

改为事务内原子更新。

2. 并发控制方案

统一采用乐观锁：

- 对 `materials`
- 对 `inventory_location_balances`
- 增加 `version` 字段
- 使用“带 version 条件的更新”避免覆盖写

不将 `SELECT ... FOR UPDATE` 作为主方案，原因是当前测试和运行环境基于 SQLite，更适合乐观锁模型。

3. 出库和调账也补锁

当前入库冲销已经有部分冲突保护思路，但库存更新入口还没有完全统一到同一套并发模型。

4. 多物料场景固定锁顺序

例如按 `material_id` 排序后逐个锁定，降低死锁概率。

### 验收标准

- 两个并发出库请求不会把库存扣成负数
- 两个并发调账请求不会覆盖彼此结果
- 并发冲销不会产生重复恢复

## P1：将查询下沉到数据库

### 目标

让库存页和出库页在数据量增大后仍能稳定工作。

### 方案

1. 重构库存总览查询

不要先全量 `findAll` 再内存筛选，应在数据库层支持：

- 仓库过滤
- 库位过滤
- 关键字过滤
- 低库存过滤

2. 重构出库列表查询

不要先查所有出库记录再内存分页，应改成数据库分页。

支持字段：

- 出库单号
- 关键字
- 操作人
- 仓库
- 库位
- 日期范围

3. 导出走专用接口

列表接口只负责分页结果；导出如果确实需要全量数据，单独走导出逻辑，不和列表复用同一查询路径。

### 结果

完成后：

- 列表性能会更稳定
- 分页准确
- 服务端压力更可控

## P2：补审计与对账能力

### 目标

让库存差异能被看见、被定位、被追溯。

### 方案

1. 增加账实校验视图

建议在库存页或后台管理页展示：

- `materials.stock_quantity`
- `sum(location_balances.quantity)`
- 差异值

2. 差异高亮和异常导出

当差异不为 `0` 时：

- 高亮显示
- 支持导出异常清单

3. 补库存影响轨迹

在入库、出库、调账详情中展示：

- 单据来源
- 仓库
- 库位
- 变化数量
- 操作人
- 原因
- 时间

4. 后续扩展能力

如果业务以后要支持更复杂的仓储动作，可以在统一模型上扩展：

- 跨库位调拨
- 自动拆分库位出库
- 批次或序列号管理

## 建议新增的数据模型

### 统一方案

新增统一流水表：

- `inventory_movements`

建议字段：

- `id`
- `material_id`
- `warehouse_id`
- `location_id`
- `source_type`
- `source_id`
- `source_line_key`
- `delta_quantity`
- `balance_after`
- `operator`
- `reason`
- `remark`
- `created_at`

所有手工调账、入库、出库、冲销、基线修复都写入这张表。

幂等约束要求：

- 对同一条业务来源明细，必须能生成稳定的幂等键
- 建议增加 `(source_type, source_id, source_line_key)` 唯一约束
- 重试、补偿、乐观锁冲突后的重复提交不得重复写流水

幂等键生成规则：

- `receipt_in` / `receipt_reversal`
  - `source_id` 使用入库单或冲销单主记录 id
  - `source_line_key` 使用订单明细 id 或 receipt item key
- `outbound` / `outbound_reversal`
  - `source_id` 使用出库单或冲销单主记录 id
  - `source_line_key` 使用出库明细 id 或物料明细键
- `manual_adjustment`
  - 先创建 adjustment 主记录
  - `source_id` 使用 adjustment id
  - `source_line_key` 固定为单明细键，如 `line:1`
- `baseline_repair`
  - 先创建 repair 主记录或 repair batch 主记录
  - `source_id` 使用 repair 或 repair batch id
  - `source_line_key` 使用物料 + 仓库 + 库位的稳定组合键

## 建议新增服务

- `server/services/inventory/inventory-movement.service.ts`
- `server/services/inventory/inventory-adjustment.service.ts`

职责建议：

- `inventory-movement.service.ts`
  负责统一库存增减、并发控制、流水写入

- `inventory-adjustment.service.ts`
  负责调账业务校验和调账接口封装

## 前端调整建议

### 库存页

- 移除直接修改库存数量入口
- 保留安全库存编辑
- 新增调账弹窗

### 调账弹窗字段建议

- 物料
- 仓库
- 库位
- 调整数量
- 调整原因
- 操作人
- 备注

### 列表能力

- 支持查看调账流水
- 支持查看库存差异
- 支持从差异视图快速发起修正

### 轨迹范围说明

- 上线后的新库存变化统一从 `inventory_movements` 查询
- 上线前的历史入库、出库、冲销不要求一次性全量回填
- 库存轨迹页应支持“统一流水 + 历史业务表”的混合查询
- 若后续决定回填历史流水，应单独作为数据治理任务推进

## 测试补齐建议

当前测试覆盖了基础入库、出库、冲销主路径，但还不足以兜住优化后的核心风险。

建议新增以下测试：

1. 调账后总库存与库位库存保持一致
2. 所有库存变更入口都走统一库存变更方法
3. 并发出库不会出现负库存
4. 并发调账不会互相覆盖
5. 库存差异检测可以识别历史脏数据
6. 基线修复后总库存与库位余额重新对齐
7. 数据库分页结果准确，不依赖内存切片
8. 同一业务来源重复提交不会产生重复库存流水

建议新增测试文件：

- `tests/inventory-adjustment-service.test.ts`
- `tests/inventory-movement-concurrency.test.ts`
- `tests/inventory-reconciliation.test.ts`

## 推荐实施顺序

### 第一阶段

1. 禁止直接修改 `stock_quantity`
2. 新增调账接口和统一流水表
3. 前端库存页增加调账能力
4. 生成差异报告，完成 `dry-run -> 人工确认 -> 正式执行` 的基线修复

### 第二阶段

1. 抽统一库存变更服务
2. 在统一库存服务中完整落乐观锁并发模型
3. 入库、出库、冲销全部接入统一入口

### 第三阶段

1. 重构库存列表和出库列表查询
2. 增加对账视图
3. 增加审计和异常导出能力

## 第一批最值得落地的 4 件事

如果只做一批高价值优化，建议优先完成：

1. 限制库存编辑接口只能修改 `min_stock`
2. 新增库存调账接口
3. 完成一次性历史库存基线修复
4. 抽统一库存变更方法并落乐观锁

## 预期收益

完成上述优化后，库存模块会从“当前可用”进入“可长期维护”的状态：

- 账本一致性更强
- 并发风险可控
- 调账行为可追溯
- 查询性能更稳定
- 后续支持调拨、批次管理等能力时成本更低
