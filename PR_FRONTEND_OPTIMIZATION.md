# Pull Request: 前端架构规范化与核心模块重构优化

## 1. 概述 (Overview)
本次 PR 是一次全面的前端重构，旨在解决代码冗余、业务逻辑散乱、UI 交互不一致以及硬编码配置项过多等技术债问题。通过提取通用的基础设施和业务 Composable，我们在保持 100% 视觉一致性的前提下，大幅提升了代码的可维护性和类型安全性。

## 2. 核心变更 (Key Changes)

### A. 配置编辑器引擎重构 (Configuration Engine)
- **基础设施提取**:
    - `useEditableList`: 统一了带 ID 行数组的 CRUD 逻辑与唯一性校验。
    - `ConfigTable.vue`: 封装了标准化的配置表格 UI（粘性表头、删除交互、布局）。
    - `configMapper.ts`: 提供了 UI 数组与后端 Map 对象之间的无损双向转换工具。
- **视图迁移**:
    - 重构了锁芯、锁叉、拉手、锁具、包装及物料目录 6 个配置页面。
    - **成果**: 视图层代码平均精简 50%-80%，逻辑从命令式转变为声明式。

### B. 核心业务模块（采购与打印）优化 (Business Logic)
- **逻辑下沉**:
    - `orderRules.ts`: 统一了全系统的订单准入校验（如打印前交货日期检查）。
    - `useOrderActions.ts`: 封装了订单状态更新、打印/导出快照、到货登记等核心高频动作。
- **引擎增强**:
    - `sheetWidthResolver.ts`: 提取了列宽适配算法 `fitPrintColumnWidths`，将复杂的打印布局计算从视图中解耦。
- **视图精简**: 极大简化了 `Procurement.vue` 和 `PrintDocument.vue` 的实现。

### C. 配置解耦与常量化 (Decoupling)
- **建立常量库**: 
    - `src/shared/constants/business.ts`: 统一管理业务默认基准值（如高度、单位、默认关键词）。
    - `src/shared/constants/storage.ts`: 规范化 LocalStorage 键名和组件间通信信号。
    - `src/shared/constants/endpoints.ts`: 集中管理 API 路由与工作流配置。
- **影响**: 彻底消除了视图层中的“魔术字符串”，降低了维护成本和拼写错误风险。

## 3. 代码质量与影响 (Impact)

- **代码量**: 累计移除冗余代码 **3400+ 行**，新增高内聚逻辑约 **900 行**。
- **类型安全**: 全量通过 `npm run type-check`，消除了多个潜在的 `any` 类型和 TS 报错。
- **一致性**: 全系统配置编辑交互、错误提示样式、打印校验流程达成高度统一。
- **性能**: 通过减少视图层复杂计算和重复 Watcher，提升了页面的响应速度。

## 4. 验证步骤 (Verification)
1. **静态检查**: 执行 `npm run type-check` 无错误。
2. **视觉比对**: 经人工走查，所有重构页面的布局、间距、字体及交互反馈与原版本 100% 一致。
3. **功能测试**:
    - 验证了 6 个配置页面的加载、编辑、JSON 导入/导出及保存发布流程。
    - 验证了采购单的筛选、分页、批量更新、到货登记及 PDF 导出功能。
    - 验证了打印预览页的模式切换与自动打印触发。

---
**分支**: `feature/frontend-optimization-20260313`
**提交记录**:
- `f941088`: 基础设施与业务 Composable 提取
- `c19b5df`: 8 个核心视图文件的重构迁移
- `91894fe`: 硬编码项常量化解耦
